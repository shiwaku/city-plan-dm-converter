{
  "name": "dm-converter-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "都市計画基本図（DMデータ）の変換結果を表示するWebビューワ",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "npm run export:style && vite",
    "build": "npm run export:style && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --nojekyll",
    "export:style": "node scripts/export-style.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^5.15.0",
    "pmtiles": "^3.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
